[pytest]
testpaths = tests
addopts = --cov=app --cov-report=term-missing --cov-fail-under=80 --strict-markers
markers =
	unit: Unit tests for isolated function and model behavior.
	integration: Integration tests that hit FastAPI endpoints with DB overrides.
